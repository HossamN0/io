export enum LANGUAGES {
    ARABIC = "ar",
    ENGLISH = "en",
}

export enum DIRECTIONS {
    RTL = "rtl",
    LTR = "ltr",
}

export enum ROUTES {
    ROOT = "",
    ABOUT = "about-us",
    TEAMS = "teams",
    BLOG = "blog",
    CONTACT = "contact-us",
    SERRVICES = "services",
    BOOK = "book-appointment",
}

export enum PAGES {
    TEST = "test",
}